package exercise;

public class LongestPalindromicSubstring {
	public String longestPalindrome(String s) {
	/*	//动态规划
		//最基础的条件
		if(s == null || s.length() == 0) {
			return "";
		}
		if(s.length() == 1) {
			return s;
		}
		String res = "";
		int maxLen = 0;
		//创建一个二维数组，存储当前子串的状态(是否回文子串)
		boolean[][] dp = new boolean[s.length()][s.length()];
		
	}
	private boolean isPalindromicSubstring() {
		
	}*/
		return s;
}
}
